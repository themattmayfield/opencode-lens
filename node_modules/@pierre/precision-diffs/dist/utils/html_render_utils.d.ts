import { PJSHighlighter, PJSThemeNames, ThemeTypes, ThemedToken, ThemesType } from "../types.js";

//#region src/utils/html_render_utils.d.ts
declare function createSpanFromToken(token: ThemedToken): HTMLSpanElement;
declare function createRow(line: number): {
  row: HTMLElement;
  content: HTMLElement;
};
interface SetupWrapperNodesProps {
  theme?: PJSThemeNames | ThemesType;
  pre: HTMLPreElement;
  highlighter: PJSHighlighter;
  split: boolean;
  wrap: boolean;
  themeType: ThemeTypes;
  diffIndicators: "bars" | "classic" | "none";
  disableBackground: boolean;
  totalLines: number;
}
interface CreateCodeNodeProps {
  pre?: HTMLPreElement;
  columnType?: "additions" | "deletions" | "unified";
}
declare function createCodeNode({
  pre,
  columnType
}?: CreateCodeNodeProps): HTMLElement;
declare function setWrapperProps({
  pre,
  highlighter,
  theme,
  split,
  wrap,
  themeType,
  diffIndicators,
  disableBackground,
  totalLines
}: SetupWrapperNodesProps): HTMLPreElement;
//#endregion
export { createCodeNode, createRow, createSpanFromToken, setWrapperProps };
//# sourceMappingURL=html_render_utils.d.ts.map