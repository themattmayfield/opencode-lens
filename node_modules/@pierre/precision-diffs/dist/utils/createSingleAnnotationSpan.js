import { getLineAnnotationName } from "./getLineAnnotationName.js";

//#region src/utils/createSingleAnnotationSpan.ts
function createSingleAnnotationSpan({ rowNumber, hunkIndex, lineIndex, annotationMap }) {
	const span = {
		type: "annotation",
		hunkIndex,
		lineIndex,
		annotations: []
	};
	for (const anno of annotationMap[rowNumber] ?? []) span.annotations.push(getLineAnnotationName(anno));
	return span.annotations.length > 0 ? span : void 0;
}

//#endregion
export { createSingleAnnotationSpan };
//# sourceMappingURL=createSingleAnnotationSpan.js.map