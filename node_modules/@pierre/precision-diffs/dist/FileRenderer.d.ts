import { BaseCodeOptions, FileContents, LineAnnotation, PJSHighlighter, ThemeTypes } from "./types.js";
import { Element, ElementContent } from "hast";

//#region src/FileRenderer.d.ts
interface FileRenderResult {
  codeAST: ElementContent[];
  preNode: Element;
  css: string;
  totalLines: number;
}
interface FileRendererOptions extends BaseCodeOptions {
  startingLineNumber?: number;
  maxLineLengthForHighlighting?: number;
}
declare class FileRenderer<LAnnotation = undefined> {
  options: FileRendererOptions;
  highlighter: PJSHighlighter | undefined;
  fileContent: string | undefined;
  constructor(options?: FileRendererOptions);
  setOptions(options: FileRendererOptions): void;
  private mergeOptions;
  setThemeType(themeType: ThemeTypes): void;
  private lineAnnotations;
  setLineAnnotations(lineAnnotations: LineAnnotation<LAnnotation>[]): void;
  cleanUp(): void;
  private computedLang;
  private queuedFile;
  private queuedRender;
  render(file: FileContents, useCSSClasses?: boolean): Promise<FileRenderResult | undefined>;
  private renderFile;
  renderFullHTML(result: FileRenderResult): string;
  renderFullAST(result: FileRenderResult, children?: ElementContent[]): Element;
  renderPartialHTML(children: ElementContent[], includeCodeNode?: boolean): string;
  private getLineNodes;
  private computeLineInfo;
  private createHastOptions;
  initializeHighlighter(): Promise<PJSHighlighter>;
}
//#endregion
export { FileRenderResult, FileRenderer };
//# sourceMappingURL=FileRenderer.d.ts.map