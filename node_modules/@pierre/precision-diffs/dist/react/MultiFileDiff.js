'use client';


import { templateRender } from "./utils/templateRender.js";
import { renderAnnotationChildren } from "./utils/renderAnnotationChildren.js";
import { useFileDiffInstance } from "./utils/useFileDiffInstance.js";
import { jsx } from "react/jsx-runtime";

//#region src/react/MultiFileDiff.tsx
function MultiFileDiff({ oldFile, newFile, options, lineAnnotations, className, style, prerenderedHTML, renderAnnotation, renderHeaderMetadata }) {
	return /* @__PURE__ */ jsx("file-diff", {
		ref: useFileDiffInstance({
			oldFile,
			newFile,
			options,
			lineAnnotations
		}),
		className,
		style,
		children: templateRender(renderAnnotationChildren({
			oldFile,
			newFile,
			renderHeaderMetadata,
			renderAnnotation,
			lineAnnotations
		}), prerenderedHTML)
	});
}

//#endregion
export { MultiFileDiff };
//# sourceMappingURL=MultiFileDiff.js.map