import { HEADER_METADATA_SLOT_ID } from "../../constants.js";
import { getLineAnnotationName } from "../../utils/getLineAnnotationName.js";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";

//#region src/react/utils/renderAnnotationChildren.tsx
function renderAnnotationChildren({ fileDiff, oldFile, newFile, renderHeaderMetadata, renderAnnotation, lineAnnotations }) {
	const metadata = renderHeaderMetadata?.({
		fileDiff,
		oldFile,
		newFile
	});
	return /* @__PURE__ */ jsxs(Fragment, { children: [metadata != null && /* @__PURE__ */ jsx("div", {
		slot: HEADER_METADATA_SLOT_ID,
		children: metadata
	}), renderAnnotation != null && lineAnnotations?.map((annotation, index) => /* @__PURE__ */ jsx("div", {
		slot: getLineAnnotationName(annotation),
		children: renderAnnotation(annotation)
	}, index))] });
}

//#endregion
export { renderAnnotationChildren };
//# sourceMappingURL=renderAnnotationChildren.js.map