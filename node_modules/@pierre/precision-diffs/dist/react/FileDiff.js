'use client';


import { templateRender } from "./utils/templateRender.js";
import { renderAnnotationChildren } from "./utils/renderAnnotationChildren.js";
import { useFileDiffInstance } from "./utils/useFileDiffInstance.js";
import { jsx } from "react/jsx-runtime";

//#region src/react/FileDiff.tsx
function FileDiff({ fileDiff, options, lineAnnotations, className, style, prerenderedHTML, renderAnnotation, renderHeaderMetadata }) {
	return /* @__PURE__ */ jsx("file-diff", {
		ref: useFileDiffInstance({
			fileDiff,
			options,
			lineAnnotations
		}),
		className,
		style,
		children: templateRender(renderAnnotationChildren({
			fileDiff,
			renderHeaderMetadata,
			renderAnnotation,
			lineAnnotations
		}), prerenderedHTML)
	});
}

//#endregion
export { FileDiff };
//# sourceMappingURL=FileDiff.js.map