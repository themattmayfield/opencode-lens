import { BaseDiffOptions, DiffLineAnnotation, FileContents, FileDiffMetadata, HunkData, HunkSeparators, RenderHeaderMetadataCallback, ThemeTypes } from "./types.js";
import { MouseEventManagerBaseOptions } from "./MouseEventManager.js";

//#region src/FileDiff.d.ts
interface FileDiffRenderProps<LAnnotation> {
  fileDiff?: FileDiffMetadata;
  oldFile?: FileContents;
  newFile?: FileContents;
  forceRender?: boolean;
  fileContainer?: HTMLElement;
  containerWrapper?: HTMLElement;
  lineAnnotations?: DiffLineAnnotation<LAnnotation>[];
}
interface FileDiffOptions<LAnnotation> extends Omit<BaseDiffOptions, "hunkSeparators">, MouseEventManagerBaseOptions<"diff"> {
  hunkSeparators?: Exclude<HunkSeparators, "custom"> | ((hunk: HunkData) => HTMLElement | DocumentFragment);
  disableFileHeader?: boolean;
  renderHeaderMetadata?: RenderHeaderMetadataCallback;
  renderAnnotation?(annotation: DiffLineAnnotation<LAnnotation>): HTMLElement | undefined;
}
declare class FileDiff<LAnnotation = undefined> {
  options: FileDiffOptions<LAnnotation>;
  private isContainerManaged;
  static LoadedCustomComponent: boolean;
  readonly __id: number;
  private fileContainer;
  private spriteSVG;
  private pre;
  private unsafeCSSStyle;
  private headerElement;
  private headerMetadata;
  private customHunkElements;
  private hunksRenderer;
  private headerRenderer;
  private resizeManager;
  private scrollSyncManager;
  private mouseEventManager;
  private annotationElements;
  private lineAnnotations;
  private oldFile;
  private newFile;
  private fileDiff;
  constructor(options?: FileDiffOptions<LAnnotation>, isContainerManaged?: boolean);
  setOptions(options: FileDiffOptions<LAnnotation> | undefined): void;
  private mergeOptions;
  setThemeType(themeType: ThemeTypes): void;
  setLineAnnotations(lineAnnotations: DiffLineAnnotation<LAnnotation>[]): void;
  cleanUp(): void;
  hydrate(props: FileDiffRenderProps<LAnnotation>): Promise<void>;
  rerender(): Promise<void>;
  handleExpandHunk: (hunkIndex: number) => void;
  render({
    oldFile,
    newFile,
    fileDiff,
    fileContainer,
    forceRender,
    lineAnnotations,
    containerWrapper
  }: FileDiffRenderProps<LAnnotation>): Promise<void>;
  private renderSeparators;
  private renderAnnotations;
  private getOrCreateFileContainer;
  getFileContainer(): HTMLElement | undefined;
  private getOrCreatePre;
  private applyHeaderToDOM;
  private setPreAttributes;
  private injectUnsafeCSS;
  private applyHunksToDOM;
}
//#endregion
export { FileDiff, FileDiffOptions };
//# sourceMappingURL=FileDiff.d.ts.map