import { DiffLineAnnotation, FileContents, LineAnnotation } from "../types.js";
import { FileOptions } from "../File.js";
import { FileDiffOptions } from "../FileDiff.js";

//#region src/ssr/FileDiffServer.d.ts
type PreloadFileOptions<LAnnotation> = {
  file: FileContents;
  options?: FileOptions<LAnnotation>;
  annotations?: LineAnnotation<LAnnotation>[];
};
interface PreloadedFileResult<LAnnotation> {
  file: FileContents;
  options?: FileOptions<LAnnotation>;
  annotations?: LineAnnotation<LAnnotation>[];
  prerenderedHTML: string;
}
type PreloadFileDiffOptions<LAnnotation> = {
  oldFile: FileContents;
  newFile: FileContents;
  options?: FileDiffOptions<LAnnotation>;
  annotations?: DiffLineAnnotation<LAnnotation>[];
};
interface PreloadedFileDiffResult<LAnnotation> {
  oldFile: FileContents;
  newFile: FileContents;
  options?: FileDiffOptions<LAnnotation>;
  annotations?: DiffLineAnnotation<LAnnotation>[];
  prerenderedHTML: string;
}
declare function preloadFile<LAnnotation = undefined>({
  file,
  options,
  annotations
}: PreloadFileOptions<LAnnotation>): Promise<PreloadedFileResult<LAnnotation>>;
declare function preloadFileDiff<LAnnotation = undefined>({
  oldFile,
  newFile,
  options,
  annotations
}: PreloadFileDiffOptions<LAnnotation>): Promise<PreloadedFileDiffResult<LAnnotation>>;
//#endregion
export { PreloadFileDiffOptions, PreloadFileOptions, PreloadedFileDiffResult, PreloadedFileResult, preloadFile, preloadFileDiff };
//# sourceMappingURL=FileDiffServer.d.ts.map